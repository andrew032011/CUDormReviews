import { useState, ChangeEvent } from 'react';
// Not finished or being used right now, but maybe in future
type Prop = {

}

const ReviewButtonSet = ({}: Prop) => {
    const createButton = () => {
        return(
            <button></button>
        )
    }
    return (
    <div>
        <button></button>
        <button></button>
        <button></button>
        <button></button>
        <button></button>
    </div>
    )
    }
export default ReviewButtonSet;