import { useState, ChangeEvent } from 'react';

type Prop = {

}

const ReviewButtonSet = ({}: Prop) => {
    const createButton = () => {
        return(
            <button></button>
        )
    }
    return (
    <div>
        <button></button>
        <button></button>
        <button></button>
        <button></button>
        <button></button>
    </div>
    )
    }
export default ReviewButtonSet;